---
:backends:
  - yaml
:hierarchy:
  - clientcert/%{clientcert}
  - puppet_role/%{puppet_role}
  - global
  - common
  - classes

:yaml:
  :datadir: /etc/puppetlabs/environments/%{environment}/hieradata
